# Insight — 2026-07-02T22:05:05.816Z

- Sessions analyzed: 1495
- Kept-code signals: 6
- Sources: autoclaw-native, cline-roo, continue, gemini

## Successful Patterns (procedural)

- Kept (git commit): 250: import android.content.Context
- Kept (git commit): private fun checkProactiveWarnings() {
- Kept (git commit): class CommandRouter(
- Kept (git commit): data class AppConfig(
- Kept (git commit): val reader = proc.inputStream.bufferedReader()

## Patterns to Avoid (failure)

- Avoid large speculative rewrites that are not backed by tests.
- Avoid adding new dependencies when an existing project utility already covers the need.

## Preferred Tools

- Gemini CLI

## Reflection

Analyzed 1495 session(s) with 6 kept-code signal(s). Preferred tools: Gemini CLI. Successful patterns: Kept (git commit): 250: import android.content.Context | Kept (git commit): private fun checkProactiveWarnings() { | Kept (git commit): class CommandRouter( | Kept (git commit): data class AppConfig( | Kept (git commit): val reader = proc.inputStream.bufferedReader(). Patterns to avoid: Avoid large speculative rewrites that are not backed by tests. | Avoid adding new dependencies when an existing project utility already covers the need..
