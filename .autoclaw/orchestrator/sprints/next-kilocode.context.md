# AutoClaw Context Pack — Sprint 1 — kilocode

_Generated 2026-07-26T20:14:58.801Z. Read this before you start: it grounds you in this project's real code, proven patterns, learned style, recent memory, and durable facts. These are retrieved hints, not authority — verify against the current code before relying on them._

**Tasks:** next-kilocode

## Grounded Context (RAG-retrieved)

**Task:** Next available work for kilocode: Auto-dispatched by orchestrator-loop. Agent kilocode is idle.

## Your Previously Successful Patterns

- Kept (git commit): 250: import android.content.Context
- Kept (git commit): private fun checkProactiveWarnings() {
- Kept (git commit): class CommandRouter(
- Kept (git commit): data class AppConfig(

## Your Learned Agent Style Guide

## Agent Style Guide

_Generated by AutoClaw `/learn`. This file is regenerated on every run from your recent sessions — edit the source signals, not this file._

## Successful Patterns

- Kept (git commit): 250: import android.content.Context
- Kept (git commit): import Supermemory from "supermemory";
- Kept (git commit): package com.omoda.lanc.ui.screens
- Kept (git commit): package com.omoda.lanc.service
- Kept (git commit): package com.omoda.lanc.mqtt
- Kept (git commit): package com.omoda.lanc.config
- Kept (git commit): data class AppUpdate(
- Kept (git commit): fun parseAndApplyLine(line: String) {
- Kept (git commit): data class AppConfig(
- Kept (git commit): val reader = proc.inputStream.bufferedReader()

## Patterns to Avoid

- Avoid large speculative rewrites that are not backed by tests.
- Avoid adding new dependencies when an existing project utility already covers the need.

## Preferred Tools

- Gemini CLI

## Project Memory Summary (recent)

pt (git commit): class CommandRouter(
- Kept (git commit): data class AppConfig(
- Kept (git commit): val reader = proc.inputStream.bufferedReader()
Patterns to avoid:
- Avoid large speculative rewrites that are not backed by tests.
- Avoid adding new dependencies when an existing project utility already covers the need.

## 2026-07-26T20:01:14.767Z — /learn
Analyzed 2152 session(s), 12 kept signal(s) from autoclaw-native, claude-code, cline-roo, continue, gemini.
Successful patterns:
- Kept (git commit): 250: import android.content.Context
- Kept (git commit): import Supermemory from "supermemory";
- Kept (git commit): package com.omoda.lanc.ui.screens
- Kept (git commit): package com.omoda.lanc.service
- Kept (git commit): package com.omoda.lanc.mqtt
- Kept (git commit): package com.omoda.lanc.config
- Kept (git commit): data class AppUpdate(
- Kept (git commit): fun parseAndApplyLine(line: String) {
- Kept (git commit): data class AppConfig(
- Kept (git commit): val reader = proc.inputStream.bufferedReader()
Patterns to avoid:
- Avoid large speculative rewrites that are not backed by tests.
- Avoid adding new dependencies when an existing project utility already covers the need.

## Instructions for the Agent

- Follow the patterns and style shown in the retrieved examples above.
- Prefer solutions that match what has been successfully kept in past sessions.
- Avoid approaches recorded under patterns to avoid.
- Match existing project conventions; read code before changing it.
- Be concise and engineering-focused; produce production-ready code.
- If the task is ambiguous, ask clarifying questions before coding.

## Durable Knowledge-Graph Facts

- *(observation)* Dispatched Next available work for kilocode (next-kilocode) to kilocode for sprint 1.
- *(observation)* Dispatched Next available work for kilocode (next-kilocode) to kilocode for sprint 1.
- *(observation)* Dispatched Next available work for kilocode (next-kilocode) to kilocode for sprint 1.
- *(observation)* Dispatched Next available work for kilocode (next-kilocode) to kilocode for sprint 1.
- *(observation)* Dispatched Next available work for kilocode (next-kilocode) to kilocode for sprint 1.
- *(observation)* Dispatched Next available work for kilocode (next-kilocode) to kilocode for sprint 1.
