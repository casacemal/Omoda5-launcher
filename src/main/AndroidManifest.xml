<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
    </queries>

    <!-- AAOS donanım zorunluluğu -->
    <uses-feature android:name="android.hardware.type.automotive" android:required="false"/>

    <!-- Araç Verileri İzinleri -->
    <uses-permission android:name="android.car.permission.CAR_SPEED" />
    <uses-permission android:name="android.car.permission.CAR_POWERTRAIN" />
    <uses-permission android:name="android.car.permission.CAR_ENERGY" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_DOORS" />
    <uses-permission android:name="android.car.permission.CAR_INFO" />

    <!-- Uygulama listeleme -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Duvar kağıdı -->
    <uses-permission android:name="android.permission.SET_WALLPAPER"/>

    <!-- Depolama — API 32 ve altı -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Depolama — API 33 ve üstü -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="ScopedStorage" />

    <!-- Overlay (Sidebar penceresi için) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- Hangi uygulama açık (AppWatcher için) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Boot'ta otomatik başlat -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- Foreground Service — tüm API seviyeleri -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- foregroundServiceType="specialUse" için API 34+ zorunlu izin -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        tools:ignore="ProtectedPermissions"/>

    <!-- ADB Connectivity Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application
        android:label="Omoda 5"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:persistent="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <uses-library android:name="android.car" android:required="false" />

        <!-- ADB Bridge Service -->
        <service
            android:name=".service.AdbBridgeService"
            android:enabled="true"
            android:exported="true" />

        <!-- Ana Launcher Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:clearTaskOnLaunch="true"
            android:stateNotNeeded="true"
            android:resumeWhilePausing="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.HOME"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!-- Split-Screen Activity -->
        <activity
            android:name=".ui.SplitScreenActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape" />

        <activity
            android:name=".ui.LauncherSettingsActivity"
            android:exported="false"
            android:screenOrientation="landscape" />

    </application>

</manifest>
