<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.omoda5.launcher"
    android:versionCode="5"
    android:versionName="5" >

    <uses-sdk
        android:minSdkVersion="24"
        android:targetSdkVersion="34" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <!-- AAOS donanım zorunluluğu -->
    <uses-feature
        android:name="android.hardware.type.automotive"
        android:required="false" />

    <!-- Araç Verileri İzinleri -->
    <uses-permission android:name="android.car.permission.CAR_SPEED" />
    <uses-permission android:name="android.car.permission.CAR_POWERTRAIN" />
    <uses-permission android:name="android.car.permission.CAR_ENERGY" />
    <uses-permission android:name="android.car.permission.CONTROL_CAR_DOORS" />
    <uses-permission android:name="android.car.permission.CAR_INFO" />

    <!-- Uygulama listeleme -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <!-- Duvar kağıdı -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <!-- Depolama — API 32 ve altı -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Depolama — API 33 ve üstü -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- Overlay (Sidebar penceresi için) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Hangi uygulama açık (AppWatcher için) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />

    <!-- Boot'ta otomatik başlat -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground Service — tüm API seviyeleri -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- foregroundServiceType="specialUse" için API 34+ zorunlu izin -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <permission
        android:name="com.omoda5.launcher.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.omoda5.launcher.DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION" />

    <application
        android:allowBackup="false"
        android:appComponentFactory="androidx.core.app.CoreComponentFactory"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Omoda 5"
        android:persistent="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar" >
        <uses-library
            android:name="android.car"
            android:required="false" />

        <!-- Ana Launcher Activity -->
        <activity
            android:name="com.omoda5.launcher.ui.MainActivity"
            android:clearTaskOnLaunch="true"
            android:exported="true"
            android:launchMode="singleTask"
            android:resumeWhilePausing="true"
            android:stateNotNeeded="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Sidebar kaldırıldı -->


        <!-- Yıldız Tuşu (294) Yakalayıcı Servis -->
        <service
            android:name="com.omoda5.launcher.services.CheryAccessibilityService"
            android:exported="true"
            android:label="Chery Star Key Service"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE" >
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Boot'ta servisi başlat -->
        <receiver
            android:name="com.omoda5.launcher.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true" >
            <intent-filter android:priority="1000" >
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="com.omoda5.launcher.androidx-startup"
            android:exported="false" >
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup" />
            <meta-data
                android:name="androidx.profileinstaller.ProfileInstallerInitializer"
                android:value="androidx.startup" />
        </provider>

        <receiver
            android:name="androidx.profileinstaller.ProfileInstallReceiver"
            android:directBootAware="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.DUMP" >
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.INSTALL_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SKIP_FILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.SAVE_PROFILE" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.profileinstaller.action.BENCHMARK_OPERATION" />
            </intent-filter>
        </receiver>
    </application>

</manifest>